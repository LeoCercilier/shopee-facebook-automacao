# DESATIVADO — substituído por .github/workflows/apis-conteudo.yml
# Mantido apenas como referência histórica. Não dispara mais.
name: "[DESATIVADO] Editorial RSS"

on:
  workflow_dispatch:
    inputs:
      confirmar_legado:
        description: "Este workflow está desativado. Use apis-conteudo.yml"
        required: true
        default: "nao"

jobs:
  desativado:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "O módulo editorial RSS foi substituído por api-conteudo/."
          echo "Use o workflow: APIs – conteúdo útil nas Páginas"
          exit 1
